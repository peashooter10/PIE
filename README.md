**PIE** (Private Interactive Environment) is a cloud&live file-sync app that allows its users to upload and download files on a server and to even sync up files, whatever their type, on different devices, from PC to Mobile, Windows, Linux or Android. <br>

**PIE** is a web-application. Its backend in Python with a MySQL database, the frontend is developed in Svelte and everything is glued together with FastAPI.

